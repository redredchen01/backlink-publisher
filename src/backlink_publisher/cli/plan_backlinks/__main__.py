# Intentionally empty: matches pre-decomposition behavior where
# `python -m backlink_publisher.cli.plan_backlinks` against the
# single-file module just imported without running main(). Real
# entry is the pyproject script `plan-backlinks = ...:main`.
